'use client'
import { useState } from 'react'
import { Shell, PageIntro, WorkCard, works } from '@/components/site-chrome'
export default function Gallery(){const [filter,setFilter]=useState('All works');const cats=['All works','Paintings','Works on paper','Prints'];const shown=filter==='All works'?works:works.filter(w=>w.category===filter);return <Shell><PageIntro eyebrow="Gallery / Selected works" title="Works that ask us to look again."><p className="intro-lede">A living archive of colour, perception and memory — browse the work by medium, then open a conversation about the piece that stays with you.</p></PageIntro><section className="gallery section-shell"><div className="gallery-heading"><p className="eyebrow">Browse the archive</p><p className="gallery-count">{shown.length} {shown.length===1?'work':'works'} selected</p></div><div className="filters" aria-label="Filter works">{cats.map(c=><button key={c} className={filter===c?'active':''} onClick={()=>setFilter(c)}>{c}</button>)}</div><div className="gallery-grid">{shown.map((work,i)=><WorkCard key={work.title} work={work} featured={i===0}/>)}</div></section></Shell>}
